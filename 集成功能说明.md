# 主程序集成功能说明

## 修改完成时间
2026年5月

## 修改内容

### 1. 已集成到 main.c 的功能

#### 1.1 阈值接收功能
- **串口接收中断**：`HAL_UART_RxCpltCallback`
- **命令解析**：支持 `TEMP:50.0`、`SMOKE:60.0`、`CO:100.0` 格式
- **阈值更新**：通过 `AlarmConfig_ParseCommand` 更新全局阈值
- **回显确认**：设置成功后返回当前配置

#### 1.2 报警检测功能
- ✅ 温度阈值检测（默认50°C）
- ✅ 烟雾阈值检测（默认60%）
- ✅ CO阈值检测（默认100ppm）
- ✅ AI风险值报警（默认0.6）
- ✅ 早期预警（提前30秒）
- ✅ 报警去抖机制（连续3次确认）

#### 1.3 报警通知功能
**二进制报警帧**：
```
[0xAA][报警类型][报警值][风险等级][0x55]
- 报警类型：0x01=温度, 0x02=烟雾, 0x03=CO, 0x04=风险
- 报警值：对应的传感器值
- 风险等级：1-5级
```

**文本报警通知**：
```
[ALARM] TEMP Temp:65.0 Smoke:50 CO:50 Risk:0.75
```

**定期数据帧**：
```
[0xFF][温度低8位][温度高8位][烟雾值][CO值][风险等级][0xFE]
每200ms发送一次
```

#### 1.4 本地报警动作
- LED指示灯：报警时常亮
- 蜂鸣器：500ms间隔闪烁
- OLED显示：显示"ALARM!"

### 2. 完整报警流程

#### 2.1 正常监控流程
```
下位机采集数据 → AI预测计算 → 本地判断 →
发送数据帧[0xFF][数据][0xFE] → 上位机接收显示
```

#### 2.2 报警触发流程
```
1. 检测到数据超限
2. 连续3次确认（去抖）
3. 触发本地报警：
   - LED常亮
   - 蜂鸣器闪烁
   - OLED显示报警
4. 发送报警通知：
   - 二进制帧：[0xAA][类型][值][等级][0x55]
   - 文本消息："[ALARM] 详细信息"
5. 上位机接收处理：
   - 解析报警类型
   - 弹窗报警提示
   - 播放报警声音
   - 控制硬件LED/蜂鸣器
```

### 3. 使用说明

#### 3.1 阈值设置（QT上位机）
```
1. 在QT界面输入新的阈值
2. 点击对应的设置按钮（温度/烟雾/CO）
3. 命令通过串口发送：TEMP:50.0\n
4. 下位机接收并更新阈值
5. 返回确认信息：[CMD] Success: TEMP:50.0
```

#### 3.2 报警解除流程
```
1. 传感器数值恢复正常
2. 下位机自动解除报警
3. 关闭LED和蜂鸣器
4. 发送解除通知：[ALARM Cleared] System Safe
5. 上位机关闭报警提示
```

### 4. 关键代码位置

| 功能模块 | 文件位置 | 函数名 |
|---------|---------|--------|
| 主循环 | main.c:100-180 | main() |
| 报警检测 | main.c:250-360 | Alarm_Check() |
| 报警通知 | main.c:495-540 | Send_Alarm_Notification() |
| 数据发送 | main.c:545-575 | Send_Data_To_PC() |
| 串口接收 | main.c:580-605 | HAL_UART_RxCpltCallback() |
| 命令处理 | main.c:610-625 | ProcessSerialCommand() |

### 5. 通信协议完整格式

#### 5.1 下位机发送格式
**常规数据帧（9字节）**：
```
字节0: 0xFF (帧头)
字节1-2: 温度值 (16位，放大10倍)
字节3-4: 烟雾值 (16位)
字节5-6: CO值 (16位)
字节7: 风险等级 (1-5)
字节8: 0xFE (帧尾)
```

**报警帧（6字节）**：
```
字节0: 0xAA (帧头)
字节1: 报警类型 (0x01-0x04)
字节2: 报警值
字节3: 风险等级
字节4: 保留
字节5: 0x55 (帧尾)
```

#### 5.2 上位机发送格式
**阈值设置命令**：
```
TEMP:50.0\n         设置温度阈值
SMOKE:60.0\n        设置烟雾阈值
CO:100.0\n          设置CO阈值
RISK:0.6\n          设置风险阈值
GET\n               获取当前配置
```

### 6. 测试建议

#### 6.1 阈值接收测试
1. 打开QT上位机
2. 连接串口（9600, 8, N, 1）
3. 发送命令：TEMP:40.0
4. 观察下位机返回确认信息

#### 6.2 报警测试
1. 调整阈值到较低值（如TEMP:30.0）
2. 加热传感器触发报警
3. 观察下位机LED/蜂鸣器
4. 观察上位机报警弹窗和声音

#### 6.3 数据传输测试
1. 查看上位机实时曲线
2. 检查数据是否正常更新
3. 验证OLED显示是否正确

### 7. 注意事项

1. **串口波特率**：9600，确保上位机一致
2. **帧同步**：检查帧头帧尾是否正确
3. **去抖机制**：需要连续3次超限才触发报警
4. **早期预警**：AI预测功能需要至少50个数据点才能准确
5. **蜂鸣器控制**：使用Buzzer引脚（PB5）

### 8. 创新点总结

1. **AI预测算法**：基于时间序列的火灾风险预测
2. **早期预警**：提前30秒预警火灾风险
3. **双向通信**：阈值设置 + 报警同步
4. **多级报警**：4级报警分级响应
5. **智能去抖**：避免误报和频繁报警

---

## 文件清单

- ✅ `main.c` - 已集成所有功能的主程序
- ✅ `alarm_config.c/h` - 报警阈值配置模块
- ✅ `fire_prediction.c/h` - AI预测算法模块
- ✅ `sensors.c/h` - 传感器采集模块

## 编译说明

使用Keil MDK编译项目，确保包含所有头文件路径。

## 联系方式

如有问题，请检查：
1. 串口连接是否正常
2. 传感器是否工作
3. 阈值是否设置正确
4. 报警通知是否收到